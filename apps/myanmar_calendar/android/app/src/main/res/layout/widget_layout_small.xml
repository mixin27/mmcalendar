<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="12dp"
    android:background="@drawable/widget_background_gradient_teal">

    <!-- Moon Phase Image - Centered -->
    <ImageView
        android:id="@+id/moon_phase_image"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="4dp"
        android:contentDescription="Moon Phase"
        android:scaleType="fitCenter" />

    <!-- Myanmar Date - Below moon -->
    <TextView
        android:id="@+id/myanmar_date"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/moon_phase_image"
        android:layout_marginTop="8dp"
        android:text="Myanmar Date"
        android:textSize="11sp"
        android:textColor="#FFFFFF"
        android:textAlignment="center"
        android:fontFamily="sans-serif-medium"
        android:lineSpacingExtra="2dp"
        android:maxLines="2"
        android:ellipsize="end" />

    <!-- Hidden views -->
    <TextView android:id="@+id/western_date" android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone" />
    <TextView android:id="@+id/moon_phase" android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone" />
    <TextView android:id="@+id/moon_day" android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone" />
    <TextView android:id="@+id/holidays" android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone" />
    <TextView android:id="@+id/astrology_info" android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone" />
    <TextView android:id="@+id/widget_title" android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone" />
    <ImageView android:id="@+id/widget_icon" android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone" android:contentDescription="Icon" />
</RelativeLayout>
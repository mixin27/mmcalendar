<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:padding="12dp"
    android:background="@drawable/widget_background_gradient_purple"
    android:weightSum="10">

    <!-- Left: Moon Phase (40%) -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="4"
        android:orientation="vertical"
        android:gravity="center"
        android:layout_marginEnd="8dp">

        <ImageView
            android:id="@+id/moon_phase_image"
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:contentDescription="Moon Phase"
            android:scaleType="fitCenter" />

        <TextView
            android:id="@+id/moon_day"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Full Moon"
            android:textSize="9sp"
            android:textColor="#FFD700"
            android:fontFamily="sans-serif-medium"
            android:layout_marginTop="4dp"
            android:textAlignment="center" />
    </LinearLayout>

    <!-- Right: Dates (60%) -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="6"
        android:orientation="vertical"
        android:gravity="center_vertical">

        <!-- Title -->
        <TextView
            android:id="@+id/widget_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Myanmar Calendar"
            android:textSize="10sp"
            android:textColor="#E0E0E0"
            android:fontFamily="sans-serif"
            android:alpha="0.8"
            android:layout_marginBottom="6dp" />

        <!-- Myanmar Date -->
        <TextView
            android:id="@+id/myanmar_date"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Myanmar Date"
            android:textSize="13sp"
            android:textColor="#FFFFFF"
            android:fontFamily="sans-serif-medium"
            android:lineSpacingExtra="2dp"
            android:maxLines="2"
            android:ellipsize="end"
            android:layout_marginBottom="3dp" />

        <!-- Western Date -->
        <TextView
            android:id="@+id/western_date"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Western Date"
            android:textSize="11sp"
            android:textColor="#E0E0E0"
            android:fontFamily="sans-serif"
            android:maxLines="1"
            android:ellipsize="end"
            android:layout_marginBottom="6dp" />

        <!-- Holiday Badge -->
        <TextView
            android:id="@+id/holidays"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🎉 Holiday"
            android:textSize="9sp"
            android:textColor="#FF6B6B"
            android:background="@drawable/holiday_badge"
            android:paddingStart="6dp"
            android:paddingEnd="6dp"
            android:paddingTop="2dp"
            android:paddingBottom="2dp"
            android:visibility="gone" />
    </LinearLayout>

    <!-- Hidden -->
    <TextView android:id="@+id/moon_phase" android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone" />
    <TextView android:id="@+id/astrology_info" android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone" />
    <ImageView android:id="@+id/widget_icon" android:layout_width="0dp" android:layout_height="0dp" android:visibility="gone" android:contentDescription="Icon" />
</LinearLayout>
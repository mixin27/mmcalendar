name: integrations_firebase
description: Firebase integration package (analytics, crashlytics, remote config).
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^4.1.1
  firebase_analytics_app:
    path: ../../features/firebase_analytics_app
  app_remote_config:
    path: ../../app_remote_config
  integrations_database:
    path: ../database

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

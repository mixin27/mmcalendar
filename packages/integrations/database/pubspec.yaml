name: integrations_database
description: Database integration package for persistence and migrations.
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  data:
    path: ../../data

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

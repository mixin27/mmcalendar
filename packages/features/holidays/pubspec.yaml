name: holidays
description: "A holidays package for Myanmar Calendar."
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.10.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_mmcalendar: ^3.2.7
  firebase_remote_config: ^6.1.3
  app_remote_config:
    path: ../../app_remote_config

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
